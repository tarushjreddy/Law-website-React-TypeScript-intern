import React from "react";

import HeaderCarousel from "../HeaderCarousel";
import Nav from "../Nav";

const Header = () => {
  return (
    <div id="home">
      <Nav />
      <HeaderCarousel />
    </div>
  );
};

export default Header;
